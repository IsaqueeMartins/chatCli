{
  "compilerOptions": {
    // ... outras opções
    "target": "es2016",
    "module": "commonjs",
    // --- Adicione/Descomente estas linhas ---
    "baseUrl": "./src" /* Especifica o diretório base para resolver nomes de módulos não-relativos. */,
    "paths": {
      "~/*": ["*"] /* Mapeia '~/' para a raiz do seu baseUrl (src/). */,
      "@app/*": [
        "app/*"
      ] /* Mapeia '@app/' para a pasta 'app/' dentro do baseUrl. */
    },
    // --- Fim das adições ---
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true
  }
}
